{{page-title "Ranking"}}

<PageLayout @title="Ranking">
    <PlayersRanking 
        @players={{this.model}} 
        @filterQuery={{this.filterQuery}}
        @updateFilterQuery={{this.updateFilterQuery}} 
    />
</PageLayout>