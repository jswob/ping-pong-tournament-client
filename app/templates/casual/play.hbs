{{page-title "Play Game"}}

<PageLayout>
    <GameHandler::MainHeadline @players={{this.model.players}} />

    <GameHandler @game={{this.model}} @onSaveGame={{perform this.saveGame this.model}} />
</PageLayout>